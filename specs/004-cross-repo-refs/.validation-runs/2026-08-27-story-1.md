# Validation run — Spec 004 quickstart Story 1 + Story 1b

**Date**: 2026-08-27
**Operator**: Martin Drechsel (single-operator Phase 1)
**Environment**: Linux 7.0.11-76070011-generic, git 2.30+, python3 3.10+.
**Cwd**: `/home/haex/Projekte/haex-hive`
**Working tree**: branch `004-cross-repo-refs` post-Phase-3 implementation
(spec-resolve tool, migrated `.haex-hive.json`, PATCHed constitution).

## Story 1 — Fresh session resolves the pinned constitution

**Setup**: `rm -rf ~/.cache/haex-hive/` (cold path exercised).

```
$ .specify/scripts/spec-resolve status
1 ref, 1 cached, last update-check: never
```

**Meaning**: 1 ref = the single `harness_sources` entry (self-role
constitution); 1 cached = self refs never need network so they count as
always cached; last update-check: never = no external fetch has ever
touched the cache directory.

```
$ .specify/scripts/spec-resolve resolve --role constitution > /tmp/const-resolved.md
$ diff /tmp/const-resolved.md <(git show 406fc786bc8c29826d2c30cd59d03c3a8235c16f:.specify/memory/constitution.md)
# (no diff output)
$ echo $?
0
```

**Meaning**: the resolved content is byte-identical to the constitution
as it existed at the pinned SHA. **SC-001 satisfied**
(device-independent resolution — the pinned SHA is what matters, not the
current working-tree state).

## Story 1b — Offline second run

Network state was not changed (this operator has network here), but the
`self`-branch of resolve does not attempt any network call, so the run
is offline-safe by construction. Re-executed the same command:

```
$ .specify/scripts/spec-resolve resolve --role constitution > /tmp/const-again.md
$ diff /tmp/const-resolved.md /tmp/const-again.md
# (no diff output)
$ sha256sum /tmp/const-resolved.md /tmp/const-again.md
4c341b748827b7e0fa0cd5433e50e042b803b08d859cde1258d223c17ea9891e  /tmp/const-resolved.md
4c341b748827b7e0fa0cd5433e50e042b803b08d859cde1258d223c17ea9891e  /tmp/const-again.md
```

**Meaning**: second run produced identical content, no network attempt,
no cache directory populated (verified: `~/.cache/haex-hive/` did not
exist after the run either). **SC-002 satisfied** (offline-safety on
cached / self refs).

## Verdict

Both story criteria pass. MVP claim ("fresh clone → constitution
resolves without path configuration; second run works offline") is
demonstrable on this repo, at this branch state, today.
