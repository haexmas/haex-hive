# Validation run — Spec 004 quickstart Story 2

**Date**: 2026-08-27
**Operator**: Martin Drechsel (single-operator Phase 1)
**Environment**: Linux, git 2.30+, python3 3.10+.
**Cwd during run**: `/tmp/spec-004-refusal-test` (scratch tree)

## Story 2 — Refusal of a reference not in the allowlist

Setup:

```
$ mkdir -p /tmp/spec-004-refusal-test
$ cp .haex-hive.json /tmp/spec-004-refusal-test/.haex-hive.json
$ cd /tmp/spec-004-refusal-test
```

Real `.haex-hive.json` in the scratch tree carries the same `harness_sources`
as this repo: a single self-role constitution entry and nothing else. That
means every external reference is refused by default.

Invocation:

```
$ /home/haex/Projekte/haex-hive/.specify/scripts/spec-resolve resolve \
    --repository "https://gitlab.com/itemis/solutions/pltf/secana-specs" \
    --revision "0000000000000000000000000000000000000000" \
    --path "README.md"
spec-resolve: refusing reference https://gitlab.com/itemis/solutions/pltf/secana-specs@0000000000000000000000000000000000000000:README.md — not permitted by any entry in harness_sources.
$ echo $?
1
```

Post-invocation state of the scratch tree:

```
$ ls -la /tmp/spec-004-refusal-test
.
..
.haex-hive.json         # unchanged
```

Nothing else was created. No file was modified. No cache directory was
populated (no external fetch was ever attempted — refusal happens
before the network branch).

## Verdict

- Exit code = 1 ✓
- Stderr names the offending reference triple ✓
- Stderr names the constraint (`not permitted by any entry in
  harness_sources`) ✓
- Zero side effects in the scratch tree ✓

**SC-006 satisfied** (Principle V mechanically enforced; the four
allowlist shapes are further exercised by
`tests/spec-resolve/test-allowlist-refusal.sh`).
