# Validation run — Spec 004 quickstart Story 4 (editor schema mapping)

**Date**: 2026-08-27
**Operator**: Martin Drechsel (single-operator Phase 1)
**Environment**: Linux, VSCode (visual verification pending); JetBrains
verification deferred with rationale (single-operator, no JetBrains
install in this environment).

## What was verified programmatically

The pieces that don't need an editor open to check:

- **Schema file exists and is at the documented path**:
  `.specify/schemas/haex-hive.schema.json` — reachable from the
  `./.specify/schemas/haex-hive.schema.json` URL used in the VSCode
  `json.schemas` snippet.
- **Schema is valid JSON**: `python3 -c "import json;
  json.load(open('.specify/schemas/haex-hive.schema.json'))"` returns 0.
- **Schema rejects the exact malformations named in the doc as
  editor-caught errors**: the shell-driven
  `tests/spec-resolve/test-schema-tool-agreement.sh` iterates every
  curated sample under `tests/spec-resolve/fixtures/config-samples/`
  (including unknown-role, `paths` on role entry, mixed-case SHA,
  `file://` scheme) and confirms my minimal Draft-07 validator
  (which stands in for VSCode's built-in one) rejects each with the
  expected error substring. VSCode's JSON language server uses the
  same Draft-07 semantics for the constructs this schema uses
  (`const`, `enum`, `pattern`, `additionalProperties: false`,
  `oneOf`, `if`/`then`, `allOf`, `not`).

## What still requires visual verification in VSCode

The last-mile check is "does the operator see a red squiggle before
they save?" — I cannot verify that from this non-interactive session.
Operator to open `.haex-hive.json` in VSCode with the mapping applied
per [`docs/spec-resolve.md`](../../../docs/spec-resolve.md) and
confirm the following show inline errors:

- Change `"role": "constitution"` to `"role": "cons"`. Expected: inline
  message `Value is not accepted. Valid values: "constitution"`.
- Remove `"revision"` from the role entry. Expected: `Missing property
  "revision"`.
- Add `"paths": ["..."]` alongside `"path"` on the role entry.
  Expected: `Property paths is not allowed` or similar (the `not:
  {required: ["paths"]}` clause in the role branch).
- Change `"revision"` to `"406FC786..."` (uppercase). Expected: pattern
  mismatch message.
- Change `"repository"` to `"file:///tmp/foo"`. Expected: `Value is not
  accepted` (the `oneOf` `pattern` branch reject).

Undo each change immediately after confirming.

## JetBrains verification — SKIPPED with rationale

Phase 1 is single-operator (Martin) working from VSCode; no JetBrains
IDE is in play in this workspace. The doc includes the JetBrains
mapping instructions (also draft-07 semantics), and the schema is
provably valid against my Draft-07 subset validator, so the
schema-side of the JetBrains claim is verified. Human-eye
confirmation on JetBrains is deferred until the second operator (or
this operator's future switch) actually uses it. This is the
skip-with-rationale path the T035 task text explicitly allows.

## Verdict (interim)

- Schema-side of the editor claim: **PASS** (verified programmatically).
- VSCode visual squiggle claim: **PENDING** (operator visual check
  required; instructions above).
- JetBrains visual squiggle claim: **DEFERRED** (out-of-scope for
  single-operator Phase 1; no failure).

Once the operator confirms the VSCode squiggle behaviour on this
branch, this file gets a follow-up "confirmed" note. Not blocking the
US4 checkpoint since the mechanism (schema + mapping instructions) is
in place and testable.
