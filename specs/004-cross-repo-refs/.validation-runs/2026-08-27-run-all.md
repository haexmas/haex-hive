# Test-suite run — `tests/spec-resolve/run-all.sh`

**Date**: 2026-08-27
**Operator**: Martin Drechsel (single-operator Phase 1)
**Environment**: Linux, git 2.30+, Python 3.10+.

## Command

```
$ cd /home/haex/Projekte/haex-hive/tests/spec-resolve
$ ./run-all.sh
```

## Summary

```
passed: 6 / 6
```

Exit code: 0.

## Per-test detail

- `test-allowlist-refusal.sh`: all four shape cases pass (shape 1 wrong
  repo, shape 2 wrong SHA, shape 3 wrong path, shape 4 role entry
  does not auto-permit other refs).
- `test-config-invalid.sh`: unknown-role, paths-on-role, bad-sha, and
  file:// scheme malformations each exit 2 with stderr pinpointing
  the offending `harness_sources[N]` entry.
- `test-prefetch.sh`: `prefetch --dry-run` on the spec-ref fixture
  prints `OK` for the self constitution ref and `MISSING` for the
  external ref.
- `test-resolve.sh`: `resolve --role constitution` on
  `consumer-with-role-only` returns byte-identical content to the
  pinned SHA's constitution.
- `test-schema-tool-agreement.sh`: all 16 curated samples (5 valid,
  11 invalid) — the minimal Draft-07 validator (schema side) and
  the tool (validator side) agree on accept/reject for every case;
  reject-case stderrs contain their `expected_error_substring`.
- `test-status.sh`: text mode produces the compact
  `1 ref, 1 cached, last update-check: never` shape; `--json` mode
  parses to a well-formed envelope with `refs_missing == 0`.

## Verdict

100% pass. FR-024, FR-025, and FR-026 (test deliverables) mechanically
satisfied. Ready to proceed to Phase 7 merge preparation.
