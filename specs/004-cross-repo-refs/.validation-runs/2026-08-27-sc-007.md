# SC-007 mechanical verification — constitution v1.1.1 + Principle V wording

**Date**: 2026-08-27
**Operator**: Martin Drechsel (single-operator Phase 1)
**File**: `.specify/memory/constitution.md`

## Version stamp

```
$ grep -n "^\*\*Version\*\*:" .specify/memory/constitution.md
200:**Version**: 1.1.1 | **Ratified**: 2026-08-26 | **Last Amended**: 2026-08-27
```

Version bumped from **1.1.0 → 1.1.1** (PATCH — pure wording change per
the version-bump rules; no principle removed, added, or relaxed).

## Principle V wording — pre + post

**Pre** (v1.1.0, historical):

> A project without an explicit `.specify/system.yaml` — or with an empty
> `external_sources.allowed` list — MUST inherit no external harness
> content …

**Post** (v1.1.1, this branch):

> A project without a `.haex-hive.json` — or with an empty
> `harness_sources` array — MUST inherit no external harness content,
> regardless of what the registry, sibling directories, sibling repos,
> or any global agent instruction file says. The registry describes
> what is *available*; the per-project `harness_sources` array grants
> *use*.

The Rationale block is unchanged. The two "Implementation guidance
for agents" blocks (Refuse-then-propose; Modifying the config
requires explicit "modify the allowlist" request) had their internal
citations rewritten from `.specify/system.yaml` /
`external_sources.allowed` to `.haex-hive.json` / `harness_sources`.
No new obligations were added; no existing obligation was weakened.

## Automated cross-check

```
$ grep -n "system.yaml\|external_sources" .specify/memory/constitution.md
(no matches beyond the Principle IV/V bodies — see full grep in
verification session)
PASS: Principle V no longer cites system.yaml or external_sources
```

## Verdict

**SC-007 satisfied**. Constitution version reads `1.1.1`, Principle
V's body cites `.haex-hive.json` / `harness_sources` throughout, no
stale citations remain. ADR 0005 (added this branch) is the
mandatory Governance-required companion for the wording change.
