# SC-008 mechanical verification — `git grep external_sources`

**Date**: 2026-08-27
**Operator**: Martin Drechsel (single-operator Phase 1)
**Command**: `git grep external_sources` on the working tree of branch
`004-cross-repo-refs` post-Phase-7 implementation (all working-tree
changes uncommitted at capture time; will be committed together per
the task-list's per-phase commit recommendation).

## Result — traceability-scoped filter

```
$ git grep -l external_sources | grep -vE '^docs/(adr|plans)/|^specs/00[1234]-'
(no matches)
```

`system.yaml` — same shape:

```
$ git grep -l "system.yaml" | grep -vE '^docs/(adr|plans)/|^specs/00[1234]-'
(no matches)
```

## Where the term does still appear (all traceability, all expected)

| Path | Reason |
|------|--------|
| `docs/adr/0002-*.md` | Original ADR that introduced the split slot + external_sources allowlist model. Superseded by ADR 0005 (this spec) but not deleted — ADRs are append-only. |
| `docs/adr/0005-*.md` | THIS spec's ADR — quotes both the pre-shape (with `external_sources`) and post-shape for reader traceability. |
| `docs/plans/2026-08-26-haex-hive-design.md` | Original design doc predating the rename. Historical. |
| `docs/plans/2026-08-27-spec-004-cross-repo-refs-design.md` | Spec 004's design doc. Documents the rename explicitly. |
| `specs/001-*/`, `specs/002-*/`, `specs/003-*/` | Historical specs from before the rename. Left intact. |
| `specs/004-cross-repo-refs/{spec,plan,research,data-model,quickstart,tasks}.md` | THIS spec's artefacts — they document the rename as part of the deliverable. Removing the term from them would remove the spec-of-record for the change. |

None of the matches fall in:

- The active `.haex-hive.json`.
- The active constitution (`.specify/memory/constitution.md`).
- Any live Python or shell script.
- Any current schema file.
- Any documentation under `docs/spec-resolve.md`.

## Verdict

**SC-008 satisfied**. Every remaining `external_sources` mention is in
a file whose job is to explain "what this used to be called and why we
renamed it". No active configuration, code, or user-facing
documentation still names the old field. The invariant "an operator
reading current files sees only `harness_sources`, not
`external_sources`" holds.
