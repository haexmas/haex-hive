# Project notes for haex-hive contributors

- When this project references sibling repositories (adjacent tools,
  shared libraries, upstream harnesses), reference them by branch name
  (`main`, `develop`) rather than by commit SHA. Pinning SHAs creates
  version-drift toil we do not want to carry.
